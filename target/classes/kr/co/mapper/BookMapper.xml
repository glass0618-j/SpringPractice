<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.mapper.BookMapper">

	<select id="getList" resultType="kr.co.domain.BookVO">
		select * from tbl_book
	</select>

	<insert id="insert">
		insert into tbl_book (book_id, book_title,
		book_writer, book_publisher, book_price, book_kategorie, book_intro,
		fileName)
		values(seq_book.nextval, #{book_title}, #{book_writer}, #{book_publisher}, #{book_price},
		#{book_kategorie}, #{book_intro}, #{fileName})
	</insert>

</mapper>